{% extends 'base.html.twig' %}
{% block title %} New client{% endblock %}
{% block body %}
    <div class="wrapper">
        <div class="main-panel">
            {{ include('navigation.html.twig') }}
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h4 class="card-title ">New User</h4>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ form_row(form.isActive, {label_attr: {class: 'checkbox-custom'} }) }}

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
