<div class="card">
    {{ form_start(form) }}

    <div class="col-md-12">
        <div class="form-row">
            <div class="form-group col-md-4">
                    {{ form_widget(form.region) }}
            </div>
            <div class="form-group col-md-4">
                    {{ form_widget(form.name) }}
            </div>
        </div>
        {{ form_widget(form.submit) }}
    </div>
    {{ form_end(form) }}

</div>
